import test from 'node:test';import assert from 'node:assert/strict';import {caption,defaults,rectAt,schedule,sanitizeEvent} from '../src/core.mjs';
test('caption puts organization beside activity date and excludes empty paragraph',()=>{const s={...defaults(),date:'2026-04-15',org:'OO 주간보호센터',activity:'봄꽃 만들기'};const p=caption(s);assert.ok(p.startsWith('4월 15일 OO 주간보호센터\n#주간보호센터 봄꽃 만들기 프로그램'));assert.ok(p.includes('#OO주간보호센터'));assert.ok(!p.includes('\n\n\n'))});
test('mixed media does not repeat short video and reserves ending',()=>{const t=schedule([{kind:'image'},{kind:'video',start:2,end:7}],{duration:30,ending:true});assert.equal(t[0].length,13.5);assert.equal(t[1].length,5);assert.equal(t[1].offset,13.5)});
test('manual mask interpolates positions and holds outside anchors',()=>{const m={points:[{t:0,x:0,y:0,w:.1,h:.2},{t:2,x:.4,y:.6,w:.3,h:.2}]};assert.deepEqual(rectAt(m,1),{x:.2,y:.3,w:.2,h:.2});assert.equal(rectAt(m,3).x,.4)});
test('telemetry drops all sensitive and free text fields',()=>{const result=sanitizeEvent({event:'error',code:'MODEL_FAILED',job:'12345678-1234-1234',phone:'01012345678',org:'비밀',filename:'어르신.jpg',description:'private',stack:'sensitive',ip:'1.2.3.4',seconds:NaN,browser:'malicious'});assert.deepEqual(result,{event:'error',version:'1.0.0',job:'12345678-1234-1234',code:'MODEL_FAILED'});assert.equal(sanitizeEvent({event:'upload',file:'sensitive'}),null)});
